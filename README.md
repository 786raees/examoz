# Examoz

examoz is an online exam taking system with the below feature

you can clone this project and run it by using the following commands

# Step 1 -> Install Packages

`cd examoz`
`pip install -r requirements.txt`

# Step 2 -> Run Migrate

`python manage.py migrate`

# Step 2 -> Run Server

`python manage.py runserver`